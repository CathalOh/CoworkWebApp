# One hostname (or regex) per line. ILIAD and org-approved connector hosts only. Regenerated by
# `python -m app.sandbox.runner` helpers or by hand. Used by tinyproxy Filter with FilterExtended.
^mock-iliad$
^iliad\.internal$
