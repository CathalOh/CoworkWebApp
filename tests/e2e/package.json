{
  "name": "cowork-e2e",
  "private": true,
  "version": "0.1.0",
  "description": "Playwright end-to-end suite for the Cowork backbone (backend + frontend)",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report report"
  },
  "devDependencies": {
    "@playwright/test": "1.56.0",
    "@types/node": "^22.7.0",
    "typescript": "^5.5.4"
  }
}
